/**
 * Auth.js v5 catch-all handler.
 * The framework's createAuth() returns GET + POST handlers; we just
 * surface them at this Next.js route.
 */
export { GET, POST } from '@synapcores/app-framework/routes/auth';
